// Server-only exports (requires next/headers)
export { createClient as createServerClient } from './supabase/server';
export { createPublicClient, createPublicClientForBuild } from './supabase/public-client';
